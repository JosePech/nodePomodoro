#!/bin/bash
if pgrep -x "nodePomodoro" > /dev/null
then
    curl -d '{"action":"show"}' -H "Content-Type: application/json" -X POST http://localhost:8080/pomodoro
else
    echo "Application not started"
fi